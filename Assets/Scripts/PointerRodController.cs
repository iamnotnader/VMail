using UnityEngine;
using System.Collections;

// TODO(nader): Was using this script to test intersections but nothing worked
// so deleted all the code except for this shell.
public class PointerRodController : MonoBehaviour {

	// Use this for initialization
	void Start () {
	}
	
	// Update is called once per frame
	void Update () {
	}
}
